# Auto-generated by Azure Portal
name: Build and deploy ASP.Net Core app to Azure Web App - simplegateway-healthcare

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4

      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Build with dotnet
        run: dotnet build SimpleGateway.csproj --configuration Release
        working-directory: ./SimpleGateway

      - name: dotnet publish
        run: dotnet publish SimpleGateway.csproj -c Release -o "${{env.DOTNET_ROOT}}/myapp"
        working-directory: ./SimpleGateway

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  deploy:
    runs-on: windows-latest
    needs: build
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_FE7CF2CCF9754DEBA1214F48B9267CBF }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_952EA1D8224B4390B572C898554681D8 }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_941B4ADEDEA84D7F98BD63E02766AACBF }}
          auth-type: SERVICE_PRINCIPAL
          enable-AzPSSession: false

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: 'simplegateway-healthcare'
          slot-name: 'Production'
          package: .
